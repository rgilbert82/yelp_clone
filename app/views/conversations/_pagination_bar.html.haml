.pagination-bar
  .current-page
    - if @count > 1
      = link_to "prev", conversations_path(page: @page, count: @count - 1), remote: true
    - else
      %span.grayed-link prev
  .page-links
    - if @count < @pages
      %span
        = link_to "next", conversations_path(page: @page, count: @count + 1), remote: true
    - else
      %span.grayed-link next
