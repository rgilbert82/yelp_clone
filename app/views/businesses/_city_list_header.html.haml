.choose-city-bar
  = render 'shared/messages'

  %h3 #{category}
  %ul
    - @cities.each do |city|
      %li.city-selector
        = link_to "#{city.format_city_state}", city_businesses_path(city)
    %li.bullet &#8226;
    %li.city-selector
      = link_to "All Cities", businesses_path
